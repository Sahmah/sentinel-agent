# Python / uv
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Tool caches
.pytest_cache/
.ruff_cache/
.hypothesis/

# Model weights (downloaded to ~/.cache/sentinel-agent/, but keep strays out)
*.pt
*.onnx

# Run output
sentinel.db
snapshots/
*.mp4

# Local Claude Code settings
.claude/settings.local.json

# Personal dev logs (Portuguese), kept local
docs/diario-*.md
docs/guide.md

# Frontend tooling, if run from the repo root by mistake
/node_modules/
/.svelte-kit/

# Dashboard bundled into the package by the release workflow
src/sentinel_agent/dashboard/

# Local test runs: reports, eval results, experiments (see report.py)
lab/
